# Community Plugins

Provider plugins for runoff. See [docs/guides/provider-plugin.md](docs/guides/provider-plugin.md) to build your own.

## Provider plugins

| Package | Backend | Mode | Notes |
|---------|---------|------|-------|
| _(none yet — be the first!)_ | | | |

## Adding your plugin

Open a PR that adds a row to the table above. Requirements:
- Published to npm with the `runoff-provider-` prefix
- `peerDependencies` includes `"runoff": ">=3.0.0"`
- README shows a working `pipeline.config.json` example

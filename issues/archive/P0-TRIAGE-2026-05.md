# P0 再分级（2026-05-26）

对照 `issue5_close.md`–`issue8_close.md` 与当前主路径（Phase 0–8 + B2–B8 已落地）。**结论：issue5–8 已全部收口（2026-05-28）；见 [OPEN-BACKLOG.md](../OPEN-BACKLOG.md)。**

## 本轮已实施

| 项 | 动作 |
|----|------|
| **6.9 DAG 缓存** | `getDagStages()` 改为始终从 `config.pipeline` 计算，避免动态扩图后命中过期 module 缓存 |
| **行业对标** | `scripts/check-benchmark-pins.ts` + `npm run check-benchmark-pins` 接入 `ci:gates`；可 `npm run refresh-benchmark-pins` 刷新 |

## P0 — 契约 & 主链（issue6）

| ID | 状态 | 依据 |
|----|------|------|
| 6.1 config 双轨制 | **已关闭** | `validateConfig` / `show-config` / 运行层均用 tuple DAG；无旧 object DSL 主路径 |
| 6.2 Provider factory | **已关闭** | `createProvider` 按 type 分支构造，非统一 `new ProviderClass(config)` |
| 6.3 mode 词面分叉 | **已关闭** | `PROVIDER_MODES` = text/agent-read/agent-write；IPC 测试用 `agent-readonly` 验证 **拒绝** 非法值 |
| 6.4 IPC SoT | **已关闭** | `npm run check-ipc-sync` 在 `ci:gates`；`insights`/`nextSteps` 已在 Python `to_dict` |
| 6.5 Python runner 退化 | **已关闭** | issue7 标 FIXED；smoke 走真实 worktree/IPC |

## P1 — 生命周期 & 编排（issue6）

| ID | 状态 | 说明 |
|----|------|------|
| 6.6 workspace resume | **已关闭** | `shouldFinalizeAgentWorkspace` 仅 `approved` 时 destroy |
| 6.7 verdict 多套逻辑 | **已关闭** | 主链 `parseVerdict` |
| 6.8 step-strategy | **已关闭** | `step-strategy.ts` + `resolveStepKind` |
| 6.9 动态 DAG 缓存 | **已关闭** | `getDagStages()` 修复 |
| 6.10 trace 生命周期 | **已关闭** | `docs/architecture/trace-lifecycle.md` |
| 6.11 全局超时取消 | **已关闭** | `executeCliRunnerTask` + abort 单测 |
| 6.12 锁语义 contract | **已关闭** | issue7 Gate1 + resilience_lock |

## P2 / P3（issue6）

| ID | 状态 |
|----|------|
| 6.13 any 泄漏 | **已关闭** |
| 6.14 原子写 | **已关闭** |
| 6.15 ownership | **已关闭** — `docs/architecture/execution-layers.md` |
| 6.16 CLIProvider union | **已关闭** |
| 6.17 typescript deps | **已关闭** — runtime `ast_utils` |
| 6.18 失败路径测试 | **已关闭** — `execution-failure-paths.test.ts` |

## issue5–issue8 文档状态（2026-05-28）

- **issue5_close … issue8_close**：全部关闭（`_close` 后缀）。
- **5.10 / 6.17**：`typescript` 保留在 `dependencies`（WONTFIX，`package.json` `comments.typescript-in-deps`）。

## P1（2026-05-26 已启动）

| 项 | 状态 | 交付 |
|----|------|------|
| 1. run-pipeline 瘦身 | **已落地** | `src/orchestration/pipeline-mcp-run.ts`；`run-pipeline.ts` ~70 行 |
| 2. Race auto-merge / llm-merge | **已落地** | `src/orchestration/race-merge.ts`；`tests/unit/race-merge.test.ts` |
| 3. Pipeline Hooks 深化 | **已落地** | `onPipelineFailed`、`trace.costSummary`、`composeEffectivePipelineContext` |

## P2（2026-05-26）

| 项 | 状态 | 交付 |
|----|------|------|
| 1. 并行 stage llm-merge | **已落地** | `stage-merge.ts` + `mergeParallelStageBranchesAsync` |
| 2. AgentGraph 导出/编辑 | **已落地** | `agent-graph-io.ts` + MCP `llm_show_agent_graph` |
| 3. Hooks + durable event-log | **已落地** | `PipelineHooks.addEventListener`；`docs/architecture/pipeline-hooks-runtime.md` |

## P3（2026-05-26）

| 项 | 状态 | 交付 |
|----|------|------|
| 1. AgentGraph 可视化 | **已落地** | `agent-graph-viz.ts`；`llm_show_agent_graph` `format=mermaid\|html` |
| 2. 外置记忆后端 | **已落地** | `memory-factory.ts` + `http-memory-client.ts`；`docs/features/external-memory.md` |
| 3. A2A 联邦 HA/认证 | **已落地** | GET directory 鉴权；sync retries + backup；`docs/features/a2a-federation.md` |

## P4（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. AgentGraph Web UI 编辑器 | **已落地** | `agent-graph-editor.ts`；`llm_show_agent_graph` `format=editor` → `~/.llm-pipeline/agent-graph-editor.html` |
| 2. Mem0/Zep 直连 | **已落地（REST）** | `mem0-memory-client.ts` / `zep-memory-client.ts`；`memoryBackend.type` `mem0` \| `zep` |
| 3. 联邦 vector-clock | **已落地（MVP）** | `federation-vector.ts`；`vector-wins` + `federationNodeId` stamp on persist |

## P5（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 编辑器 waves + 环检测 | **已落地** | `agent-graph-validate.ts`；编辑器 waves 区 + `apply` 拒绝环 |
| 2. Mem0/Zep SDK 可选层 | **已落地（lazy）** | `memory-transport.ts`；`transport: rest\|sdk\|auto`（无 lockfile 新依赖） |
| 3. 联邦 quorum | **已落地（MVP）** | `federation-quorum.ts`；`federationQuorumMin` |

## P6（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 编辑器拖拽 + Mermaid 双向 | **已落地** | `agent-graph-editor.ts`；`agent-graph-mermaid-parse.ts`；`apply` 支持 `mermaid=` |
| 2. SDK 集成测试 | **已落地** | `tests/integration/memory-sdk.integration.test.ts`；`npm run test:sdk-memory`；`CI_SDK_MEMORY=1` |
| 3. 联邦 leader 选举 | **已落地（MVP）** | `federation-leader.ts`；`federationLeaderElection`；leader 才 push |

## P7（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. DAG 画布编辑器 | **已落地** | `agent-graph-canvas.ts`；`format=canvas` → `~/.llm-pipeline/agent-graph-canvas.html` |
| 2. 联邦 lease | **已落地** | `federation-lease.ts`；`federationLeaderLease` + `federationLeaseMs` |
| 3. CRDT 合并算子 | **已落地** | `federation-crdt.ts`；`federationConflictStrategy: crdt-merge` |

## P8（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 画布坐标 + 缩放平移 | **已落地** | `AgentGraphSnapshot.layout/view`；canvas 拖拽/滚轮/Shift+平移 + localStorage |
| 2. Lease 心跳续期 | **已落地** | `federation-lease-heartbeat.ts`；`HttpA2ATransport` 启动时后台 renew |
| 3. 墓碑 CRDT 传播 | **已落地** | `createAgentCardTombstone`；`filterActiveAgentCards`；metadata LWW + auth 合并 |

## P9（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 画布 PNG + 自动布局 | **已落地** | `agent-graph-layout.ts`；canvas `Auto layout` / `Export PNG` |
| 2. Lease 见证 + split-brain | **已落地** | `federation-lease-witness.ts`；GET `/a2a/federation/lease`；sync 告警 |
| 3. 删除 API + 墓碑 GC | **已落地** | `federation-delete.ts`；`deleteFederatedAgentCard`；`compactFederationTombstones` |

## P10（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 画布边编辑 + 撤销栈 | **已落地** | 点击边选中/删除；Undo/Redo + ⌘Z；`pushHistory` |
| 2. Lease 仲裁 + 自动降级 | **已落地** | `federation-lease-arbitration.ts`；`releaseFederationLease`；sync 降级 |
| 3. Skill 粒度 tombstone | **已落地** | `federationSkillTombstones`；`deleteFederatedAgentSkill` |

## P11（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 画布多选 + 对齐辅助线 | **已落地** | Shift+多选；拖拽吸附 SNAP；虚线对齐参考 |
| 2. Lease 写入 quorum 见证 | **已落地** | `federation-lease-quorum.ts`；`lease-quorum-witnesses.json`；GET `/lease/witnesses` |
| 3. Skill tombstone GC | **已落地** | `gcSkillTombstonesOnAgents`；`compactFederationTombstones` 双计数 |

## P12（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 画布框选 + 对齐分布 | **已落地** | 拖拽框选；Align L/C/R/T/M/B；Distribute H/V |
| 2. Witness POST 回执 | **已落地** | POST `/a2a/federation/lease/witness`；`broadcastLeaseWitnessToPeers` |
| 3. Skill 级 quorum 合并 | **已落地** | `federation-skill-quorum.ts`；`federationSkillQuorumMin` |

## P13（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 画布分组 + 锁定节点 | **已落地** | `AgentGraphSnapshot.nodeMeta`；`selGroup` / `selLocked`；锁定不可拖拽 |
| 2. Lease 见证链审计日志 | **已落地** | `federation-lease-audit.ts`；`lease-audit-chain.json`；GET `/lease/audit` |
| 3. 跨 agent skill 依赖图 | **已落地** | `federation-skill-deps.ts`；`federationSkillDeps` CRDT union；环检测 |

## P14（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 画布折叠分组 + 组内批量锁定 | **已落地** | `collapsedGroups`；▸/▾ 标签折叠；`Lock group` |
| 2. Audit 链 HMAC 签名 + 导出 | **已落地** | `seal` + `verifyLeaseAuditSeal`；GET `/lease/audit/export` |
| 3. Skill 依赖环阻断同步 | **已落地** | `validateFederationSkillDependencies`；`federationSkillDepsBlockSync` |

## P15（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 组级对齐 + 组拖拽 | **已落地** | `dragIdsForNode`；`alignActiveGroup`；Grp L/C/R/T/M/B |
| 2. Audit 链密钥轮转 | **已落地** | `federation-lease-audit-keys.ts`；`lease-audit-keyring.json`；POST `/audit/rotate` |
| 3. Skill 环自动剪枝 | **已落地** | `pruneSkillDependencyCycles`；`federationSkillDepsPruneSync` |

## P16（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 嵌套分组 + 组间连线 | **已落地** | `parentGroup`；`groupLinks`；`Link groups`；紫色组间虚线 |
| 2. Audit  detached 签名包 | **已落地** | `federation-lease-audit-export.ts`；GET `/audit/export?format=bundle` |
| 3. 剪枝策略可配置 | **已落地** | `oldest-dep` / `min-edge` / `last-edge`；`federationSkillDepsPruneStrategy` |

## P17（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 组链接编辑/删除 + 嵌套折叠 | **已落地** | 选中组连线编辑；Delete 删除；`collapsedParents` |
| 2. bundle 离线验签 CLI | **已落地** | `verifyLeaseAuditSignedBundle`；`npm run verify-lease-audit-bundle` |
| 3. 剪枝日志落盘 + 同步回执 | **已落地** | `skill-deps-prune-log.json`；`skillDepsPruneReceipt` |

## P18（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 组链接 Mermaid + 父组拖拽 | **已落地** | `agentGraphGroupLinksToMermaid`；canvas `copyGroupMermaid`；`startParentGroupDrag` |
| 2. prune/audit 日志 GET API | **已落地** | GET `/skill-deps/prune-log`；GET `/lease/audit/log?limit=` |
| 3. 剪枝策略 per-agent 覆盖 | **已落地** | `federationSkillDepsPruneStrategy` on card metadata |

## P19（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 组链接 Mermaid 导入 | **已落地** | `parseAgentGraphGroupLinksFromMermaid`；canvas `fromGroupMermaid` |
| 2. prune-log 按 agent 过滤 | **已落地** | GET `/skill-deps/prune-log?agentId=` |
| 3. 剪枝策略 CRDT merge | **已落地** | `mergeSkillDepPruneStrategyCrdt`（冲突 → min-edge） |

## P20（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 组链接悬空校验 | **已落地** | `findDanglingGroupKeys`；canvas status；`validateAgentGraphSnapshot` |
| 2. audit/prune 日志 NDJSON | **已落地** | `?format=ndjson` on `/lease/audit/log` and `/skill-deps/prune-log` |
| 3. 剪枝策略向量 tie-break | **已落地** | `mergeSkillDepPruneStrategyCrdt(..., { a, b })` |

## P21（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 悬空组一键修复 | **已落地** | `repairDanglingGroupLinks`；canvas `fixDanglingRemove` / `fixDanglingPlaceholder` |
| 2. prune-log receiptId 查询 | **已落地** | GET `/skill-deps/prune-log?receiptId=` |
| 3. 剪枝策略变更审计 | **已落地** | `skill_prune_strategy` on lease audit chain |

## P22（2026-05-26）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 占位节点样式区分 | **已落地** | `nodeMeta.placeholder`；canvas 琥珀色虚线框 |
| 2. audit type 过滤 | **已落地** | GET `/lease/audit/log?type=skill_prune_strategy` |
| 3. 策略 LWW 回滚 | **已落地** | `rollbackSkillDepPruneStrategyCrdt`；POST `/prune-strategy/rollback` |

## P23（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 占位节点一键删除 | **已落地** | `removePlaceholderGraphNodes`；canvas `Remove placeholders` |
| 2. audit 多 type 过滤 | **已落地** | `parseLeaseAuditEventTypes`；`?type=acquire,skill_prune_strategy` |
| 3. 回滚后关闭 rollback 模式 | **已落地** | `applySkillDepPruneStrategyRollbackForAgent` → `configureSkillDepPruneStrategyRollback(false)` |

## P24（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 删除占位后 recompute waves | **已落地** | `removePlaceholderGraphNodes` + canvas `recomputeWavesClient` |
| 2. audit exclude 过滤 | **已落地** | GET `/lease/audit/log?exclude=acquire` |
| 3. rollback 失败保留模式 | **已落地** | 仅成功时 `configureSkillDepPruneStrategyRollback(false)`；显式 `reason` |

## P25（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 删除占位后 auto-layout | **已落地** | `computeDagWaveLayout` after remove；canvas `layoutNodes()` |
| 2. type+exclude 组合校验 | **已落地** | `validateLeaseAuditLogFilterOptions`；重叠 → HTTP 400 |
| 3. rollback HTTP rollbackMode | **已落地** | POST 响应 `rollbackMode: isSkillDepPruneStrategyRollbackEnabled()` |

## P26（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 删除占位 undo 提示 | **已落地** | canvas status：`Removed N placeholder(s) — Undo ⌘Z` |
| 2. audit 空结果提示 | **已落地** | `filteredEmpty` + `emptyHint` on filtered GET |
| 3. rollback priorMode | **已落地** | POST 响应 `priorMode`（变更前进程状态） |

## P27（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. undo 常驻 footer | **已落地** | canvas `<footer>Undo ⌘Z · Redo ⇧⌘Z` |
| 2. prune-log 空结果提示 | **已落地** | `skillDepsPruneLogEmptyHint`；`filteredEmpty` + `emptyHint` |
| 3. enable-only 无 applied | **已落地** | POST rollback 无 `agentId` 时不序列化 `applied` |

## P28（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. footer undo 栈深度 | **已落地** | `updateUndoFooter()` · `#undoFooterHint` stack count |
| 2. receipt 未找到提示 | **已落地** | `emptyHintCode: receipt_not_found` + `receiptNotFound: true` |
| 3. rollback 失败含 priorMode | **已落地** | POST 失败路径仍返回 `priorMode`（请求前快照） |

## P29（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. footer redo 栈深度 | **已落地** | `updateUndoFooter`：`undo: N · redo: M` |
| 2. audit emptyHintCode | **已落地** | `audit_chain_empty` / `no_events_match_filter` |
| 3. rollback ok 语义 | **已落地** | `ok: false` 当 `applied.ok === false` |

## P30（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. footer MAX_HISTORY | **已落地** | `undo: N/40` in `#undoFooterHint` |
| 2. audit NDJSON meta 行 | **已落地** | 空过滤结果首行 `{ _meta: true, filteredEmpty, ... }` |
| 3. rollback reasonCode | **已落地** | `SkillDepPruneRollbackReasonCode` on `applied` |

## P31（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. undo 满栈 footer 高亮 | **已落地** | `#canvasFooter.undo-stack-full` 琥珀色样式 |
| 2. prune-log NDJSON meta | **已落地** | `exportSkillDepsPruneLog` 空过滤 `_meta` 行 |
| 3. rollback 成功 reasonCode | **已落地** | `reasonCode: "applied"` on success |

## P32（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 满栈 status oldest drop | **已落地** | `pushHistory` → `#status[data-kind=warn]` |
| 2. 空 NDJSON 统一 schema | **已落地** | `federation-ndjson-meta.ts` `federation-log-empty-v1` |
| 3. rollback reasonCode 表 | **已落地** | `docs/features/a2a-federation.md` |

## P33（2026-05-27）— 已落地

| 项 | 状态 | 落点 |
|----|------|------|
| 1. 满栈 warn dismiss / 自动 ok | **已落地** | `updateGraphStatus`；非 drop 的 `pushHistory` / 点击 `#status` |
| 2. NDJSON meta export 断言 | **已落地** | `tests/federation/federation-ndjson-meta.test.ts` 双 export 同 schema keys |
| 3. rollback audit reasonCode | **已落地** | `skill_prune_strategy_rollback` on lease audit chain |

## P 系列 2026-05 收口

- **范围**：`P1`–`P33`（33 批，每批约 3 项；末批 P23–P33 为画布 undo/占位 + 联邦 prune/audit HTTP + CRDT rollback 抛光）。
- **状态**：**全部已落地**；**不再开设 P34+**。
- **后续**：技术债与产品演进见上文 **issue6 部分项**、**ROADMAP Phase 9+**；新需求单独开 issue/里程碑，不延续 P 编号微循环。
